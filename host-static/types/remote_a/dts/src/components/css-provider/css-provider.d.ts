import "../../styles/global.css";
import { ReactNode } from "react";
type CssProviderProps = {
    children: ReactNode;
};
export declare const CssProvider: ({ children }: CssProviderProps) => JSX.Element;
export {};
